# База данных

## Технологии

- PostgreSQL — основное хранилище данных.
- Prisma ORM — типобезопасный доступ к таблицам.
- Миграции Prisma (`prisma/migrations`) синхронизируют схему между окружениями.

## Основные сущности

| Сущность                 | Описание                                                               |
| ------------------------ | ---------------------------------------------------------------------- |
| `SurveySubmission`       | Агрегированная анкета после прохождения мастера с ключевыми признаками |
| `RecommendationSnapshot` | Сохранённая карточка рекомендации (секция, score, rank, причины)       |
| `SectionCatalog`         | Статический каталог спортивных секций из `src/data/sections.ts`        |
| `Interaction`            | (планируется) события интереса пользователя к секции                   |

## Диаграмма «сущность-связь»

Полная ER-диаграмма вынесена в `docs/ARCHITECTURE.md` (раздел «Модель данных»). Поскольку текущая Prisma-схема содержит только `SurveySubmission` и `RecommendationSnapshot`, остальные сущности отмечены как планы развития и требуют отдельных миграций.

## Работа с миграциями

```powershell
# Применить существующие миграции
npx prisma migrate deploy

# Сгенерировать новую миграцию после изменения schema.prisma
npx prisma migrate dev --name add-new-field
```

> 💡 **Совет:** перед созданием новой миграции убедитесь, что схема согласована с ER-моделью и обновите документацию при добавлении новых сущностей.

## Дамп и восстановление

```powershell
# Создать дамп (пример для psql)
pg_dump --format=custom --file=backup.dump "$env:DATABASE_URL"

# Восстановить дамп
pg_restore --clean --if-exists --dbname="$env:DATABASE_URL" backup.dump
```

> ⚠️ **Внимание:** команды выше перезаписывают существующие данные. Выполняйте их только на резервных или тестовых окружениях.
