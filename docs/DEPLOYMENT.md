# Руководство по развёртыванию

## Предпосылки

- Node.js 18+ и npm
- PostgreSQL 14+ (локально или в облаке)
- Доступ к токену Telegram-бота (BotFather)
- Утилита `npx prisma` (ставится вместе с зависимостями)

## Переменные окружения

| Переменная                     | Назначение                                                                |
| ------------------------------ | ------------------------------------------------------------------------- |
| `BOT_TOKEN`                    | Токен Telegram-бота                                                       |
| `DATABASE_URL`                 | Строка подключения к PostgreSQL                                           |
| `ADMIN_USERNAME`               | Имя пользователя для входа в админ-панель                                 |
| `ADMIN_PASSWORD`               | Пароль администратора (не используется, если задан `ADMIN_PASSWORD_HASH`) |
| `ADMIN_PASSWORD_HASH`          | Готовый Argon2-хэш пароля                                                 |
| `ADMIN_SESSION_SECRET`         | Секрет для cookie-сессий (минимум 32 символа)                             |
| `ADMIN_SESSION_TTL_SECONDS`    | Время жизни админ-сессии в секундах (по умолчанию 86400 = 24 часа)        |
| `FASTIFY_PORT` _(опционально)_ | Порт сервера (по умолчанию 3000)                                          |
| `FASTIFY_HOST` _(опционально)_ | Интерфейс привязки (по умолчанию `0.0.0.0`)                               |

> ⚠️ **Важно:** никогда не коммитьте `.env` в репозиторий. Используйте менеджеры секретов при деплое в облако.

## Развёртывание в режиме разработки

1. Скопируйте базовый `.env`:

   ```powershell
   Copy-Item .env.example .env
   ```

2. Заполните минимум `BOT_TOKEN`, `DATABASE_URL`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`, `ADMIN_SESSION_SECRET`.
3. Установите зависимости:

   ```powershell
   npm install
   ```

4. Примените миграции:

   ```powershell
   npx prisma migrate deploy
   ```

   Если вы изменяли `schema.prisma` (добавляли новые поля или таблицы), выполните локально также:

   ```powershell
   npx prisma migrate dev --name "add_preferred_times_and_events"
   npx prisma generate
   ```

5. Запустите сервер и бота:

   ```powershell
   npm run dev
   ```

6. Для работы админ-панели одновременно поднимите Vite-dev сервер:

   ```powershell
   npm run dev:admin
   ```

## Продакшен-развёртывание

1. Соберите проект:

   ```powershell
   npm run build
   ```

2. Примените миграции в прод-среде:

   ```powershell
   npx prisma migrate deploy
   ```

3. Запустите приложение:

   ```powershell
   npm start
   ```

> 💡 **Совет:** в контейнерных окружениях установите переменную `NODE_ENV=production` и используйте процесс-менеджер (PM2, systemd, Heroku dyno).

## Дополнительные замечания

- Для деплоя на Heroku используйте `Procfile`, который уже входит в репозиторий.
- Изображения секций должны быть доступны приложению; при деплое убедитесь, что директория `dist/src/data/images` содержит необходимые ассеты.
- Если требуется устойчивость сессий бота, рассмотрите подключение Redis и замену in-memory хранилища.

## Конфигурация движка рекомендаций и ML

- `RECOMMENDER_MODE` — режим ранжирования. Поддерживается:

  - `rules` — rule‑based (по умолчанию);
  - `tfidf` — content‑based TF‑IDF (лексическое сопоставление профиля ↔ описание секции);
  - `hybrid` — гибридное ранжирование: `ruleScore * ALPHA + tfidfScore * BETA`.

- `RECOMMENDER_ALPHA` — вес rule‑score в гибридном режиме (0..1), по умолчанию `0.6`.
- `RECOMMENDER_BETA` — вес tfidf‑score в гибридном режиме (0..1), по умолчанию `0.4`.

Эти переменные можно настроить в `.env` или на хостинге через менеджер окружения.

Для лучшего соответствия русскоязычных описаний секций можно установить стеммер:

```powershell
npm install snowball-stemmer.jsx
```

Движок автоматически импортирует пакет, если он доступен.

## Сбор фидбека и экспорт данных для обучения

Мы сохраняем в базе:

- `SurveySubmission` — профили и snapshot рекомендованных секций;
- `RecommendationEvent` — события `impression`, `click`, `conversion` и `feedback` с полем `payload`.

Эти данные используются для оффлайн‑обучения Learning‑to‑Rank и оценки метрик (CTR, конверсия).

Экспорт выборки для обучения доступен через npm‑скрипт:

```powershell
npm run export:training
```

Скрипт экспортирует CSV со столбцами: `age, gender, fitnessLevel, preferredFormats, desiredGoals, sectionId, rank, score, popularity, clicked, converted`.
